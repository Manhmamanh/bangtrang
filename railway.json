{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd packages/server && npm install && npm start",
    "restartPolicyCondition": "on-failure",
    "restartPolicyMaxRetries": 5
  }
}
